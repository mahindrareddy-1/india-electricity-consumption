Save your Tableau workbook here as `electricity_dashboard.twbx`
(File > Save As in Tableau Desktop, "Packaged Workbook" format).

Packaging as .twbx (not .twb) bundles any extract data with the workbook,
so the file is self-contained and safe to commit to Git / hand in as a
submission.
